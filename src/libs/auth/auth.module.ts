import { NgModule } from '@angular/core';
import { AuthenticatingComponent } from './components';

@NgModule({
  declarations: [AuthenticatingComponent],
  exports: [AuthenticatingComponent],
})
export class AuthModule { }
